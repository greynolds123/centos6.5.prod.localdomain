#
# A virtual host using mix of IP-, name-, and port-based configuration
#

server {
    listen       8080;
    listen       centos6.5.prod.localdomain:8280;
    server_name  centos6.5.prod.localdomain  alias  centos6.5.alias;

    location / {
        root   html;
        index  index.html index.htm;
    }
}

